# Normalize line endings to LF in the repo (Vercel/Linux build)
* text=auto eol=lf

# Treat these as binary (no line-ending conversion)
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.gif binary
